import SectionHeader from "@/components/SectionHeader";

const Advertisement = () => {
  const breadcrumbs = ["Advertisement"];
  return <SectionHeader title="Advertisement" breadcrumbs={breadcrumbs} />;
};

export default Advertisement;
